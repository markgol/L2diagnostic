[window]
geometry=@ByteArray()	; default, will be filled automatically on first run

[net]
srcIP=192.168.1.2	; default local IP to bind
dstIP=192.168.1.62	; default L2 IP (no DHCP possible)
srcPort=6201		; default UDP listen port
dstPort=6101		; default UDP talk port

[record]
csv=false		; default CSV recording disabled
